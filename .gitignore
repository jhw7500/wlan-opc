*.o
*.a

# per-arch build artifacts (build/native, build/arm64) — see top-level Makefile
build/

# OMC harness scratch (never tracked)
.omc/
.bkit/
.gstack/

# local test reports / scratch (untracked)
tmp/

# agent / tooling scratch
.agents/
skills-lock.json
*.swp

# docs: spec copies (canonical lives in parent wlan-package/docs/) + binary originals
docs/*.docx
docs/seed.yaml
docs/proto-todo.md
docs/opc_vhl_protocol_Rev1.00_KO.md
docs/manual-runthrough.md
docs/spec-conformance.pdf

# docs: 외부사(DFK) 미팅 자료 — 보안상 git 추적 제외 (로컬 보존)
docs/dfk-meeting/

# Compound Engineering tooling (machine-local)
.compound-engineering/

# on-target 테스트 환경값 (실비밀번호/사내 IP 포함 가능 — 스키마는 test-env.json.example 참조)
test-env.json
